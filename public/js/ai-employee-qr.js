(()=>{const $=id=>document.getElementById(id);let point=null,points=[];const auth=()=>({Authorization:`Bearer ${token}`,'Content-Type':'application/json'});function agentSlug(){const a=typeof lastData!=='undefined'&&lastData?.agent||{};return a.public_slug||a.slug||a.share_slug||''}function safe(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}async function request(url,opt={}){const r=await fetch(url,{...opt,headers:{...auth(),...(opt.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw Error(d.error||`İşlem başarısız (${r.status})`);return d}async function makePoint(){const name=($('qrPointName')?.value||'').trim();if(!name)return Swal.fire('Nokta adı gerekli','Örn: Vitrin, Bekleme Salonu veya Masa 7','info');const b=$('qrCreate');b.disabled=true;b.textContent='Oluşturuluyor…';try{const d=await request(`${API}/agents/${encodeURIComponent(id)}/qr-points`,{method:'POST',body:JSON.stringify({point_name:name})});point=d;$('qrPointName').value='';refresh();await loadPoints();Swal.fire({icon:'success',title:'QR noktası oluşturuldu',text:`${d.point_name} · ${d.serial}`,timer:1400,showConfirmButton:false})}catch(e){Swal.fire('QR oluşturulamadı',e.message,'error')}finally{b.disabled=false;b.textContent='＋ Yeni QR Noktası Oluştur'}}function link(p=point){const s=p?.slug||agentSlug();if(!s||!p?.serial)return'';return `${location.origin}/l/${encodeURIComponent(s)}?src=${encodeURIComponent(p.serial)}`}function refresh(){if(!$('qrCode'))return;const u=link();$('qrSerial').textContent=point?.serial||'CC-——';$('qrCode').innerHTML='';if(u&&window.QRCode)new QRCode($('qrCode'),{text:u,width:250,height:250,colorDark:'#000000',colorLight:'#ffffff',correctLevel:QRCode.CorrectLevel.H})}function exportCanvas(){const c=document.createElement('canvas');c.width=1200;c.height=1380;const x=c.getContext('2d');x.fillStyle='#fff';x.fillRect(0,0,c.width,c.height);const q=$('qrCode').querySelector('canvas')||$('qrCode').querySelector('img');if(q)x.drawImage(q,100,80,1000,1000);x.fillStyle='#111827';x.textAlign='center';x.font='700 54px Arial';x.fillText(point?.serial||'',600,1190);return c}function ready(){if(!point){Swal.fire('QR seç','Yeni QR oluştur veya QR Noktalarım listesinden bir QR seç.','info');return false}return true}function dl(u,n){const a=document.createElement('a');a.href=u;a.download=n;document.body.appendChild(a);a.click();a.remove()}async function jpg(){if(!ready())return;dl(exportCanvas().toDataURL('image/jpeg',1),`${point.serial}.jpg`)}async function pdf(){if(!ready())return;if(!window.jspdf)return Swal.fire('PDF aracı yüklenemedi');const c=exportCanvas(),{jsPDF}=window.jspdf,p=new jsPDF({orientation:'portrait',unit:'mm',format:'a4'});p.addImage(c.toDataURL('image/png'),'PNG',20,25,170,195);p.save(`${point.serial}.pdf`)}async function printIt(){if(!ready())return;const u=exportCanvas().toDataURL('image/png'),w=window.open('','_blank');if(!w)return Swal.fire('Uyarı','Popup engelini kontrol et.','warning');w.document.write(`<style>@page{size:A4;margin:12mm}body{margin:0;display:grid;place-items:center}img{width:170mm;height:auto}</style><img src="${u}" onload="print()">`);w.document.close()}function ensureArchive(){if($('qrArchive'))return;const sec=$('qr')||$('qrCode')?.closest('.panel');if(!sec)return;const box=document.createElement('div');box.className='box';box.id='qrArchive';box.style.marginTop='16px';box.innerHTML='<div style="display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:12px"><h2 style="margin:0">QR Noktalarım</h2><span class="hint" id="qrCount"></span></div><div class="list" id="qrPointList"><div class="empty">QR noktaları yükleniyor…</div></div>';sec.appendChild(box);$('qrPointList').addEventListener('click',archiveClick)}function renderPoints(){ensureArchive();$('qrCount').textContent=`${points.length} kayıt`;const el=$('qrPointList');if(!points.length){el.innerHTML='<div class="empty">Henüz QR noktası oluşturulmadı.</div>';return}el.innerHTML=points.map(p=>`<div class="row" data-qid="${p.id}" style="opacity:${p.is_active===false?'.58':'1'}"><div class="meta"><b style="color:var(--text,#111827)">${safe(p.point_name)}</b><span>${p.is_active===false?'Pasif':'Aktif'}</span></div><div class="preview" style="font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-weight:800">${safe(p.serial)}</div><div class="hint" style="margin-top:6px">${Number(p.scan_count||0).toLocaleString('tr-TR')} tarama · ${Number(p.conversations||0).toLocaleString('tr-TR')} sohbet${p.last_scanned_at?` · Son tarama ${new Date(p.last_scanned_at).toLocaleDateString('tr-TR')}`:''}</div><div class="crm-actions"><button data-act="show">QR Gör</button><button data-act="jpg">JPEG</button><button data-act="pdf">PDF</button><button data-act="rename">Yeniden Adlandır</button><button data-act="toggle">${p.is_active===false?'Aktif Yap':'Pasif Yap'}</button></div></div>`).join('')}async function loadPoints(){ensureArchive();try{const d=await request(`${API}/agents/${encodeURIComponent(id)}/qr-points`);points=Array.isArray(d.points)?d.points:[];renderPoints()}catch(e){$('qrPointList').innerHTML=`<div class="empty">QR noktaları alınamadı: ${safe(e.message)}</div>`}}function choose(p){point={...p,slug:agentSlug()};refresh();$('qrCode')?.scrollIntoView({behavior:'smooth',block:'center'})}async function archiveClick(e){const btn=e.target.closest('button[data-act]');if(!btn)return;const row=btn.closest('[data-qid]'),p=points.find(x=>String(x.id)===row?.dataset.qid);if(!p)return;const act=btn.dataset.act;if(['show','jpg','pdf'].includes(act)){choose(p);if(act==='jpg')setTimeout(jpg,80);if(act==='pdf')setTimeout(pdf,80);return}if(act==='rename'){const r=await Swal.fire({title:'QR noktasını yeniden adlandır',input:'text',inputValue:p.point_name||'',showCancelButton:true,confirmButtonText:'Kaydet',cancelButtonText:'Vazgeç',inputValidator:v=>!String(v||'').trim()?'Nokta adı gerekli':undefined});if(!r.isConfirmed)return;try{await request(`${API}/agents/${encodeURIComponent(id)}/qr-points/${encodeURIComponent(p.id)}`,{method:'PUT',body:JSON.stringify({point_name:String(r.value).trim()})});await loadPoints();if(point?.id===p.id){point={...point,point_name:String(r.value).trim()}}}catch(err){Swal.fire('Güncellenemedi',err.message,'error')}return}if(act==='toggle'){const next=p.is_active===false;const r=await Swal.fire({title:next?'QR aktif edilsin mi?':'QR pasif yapılsın mı?',text:next?'Bu fiziksel QR yeniden kullanılabilir hale gelir.':'Kayıt ve geçmiş analitik silinmez.',icon:'question',showCancelButton:true,confirmButtonText:next?'Aktif Yap':'Pasif Yap',cancelButtonText:'Vazgeç'});if(!r.isConfirmed)return;try{await request(`${API}/agents/${encodeURIComponent(id)}/qr-points/${encodeURIComponent(p.id)}`,{method:'PUT',body:JSON.stringify({is_active:next})});await loadPoints()}catch(err){Swal.fire('Güncellenemedi',err.message,'error')}}}function init(){if(!$('qrCode'))return;ensureArchive();$('qrCreate')?.addEventListener('click',makePoint);$('qrPdf')?.addEventListener('click',pdf);$('qrJpeg')?.addEventListener('click',jpg);$('qrPrint')?.addEventListener('click',printIt);refresh();loadPoints()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init()})();